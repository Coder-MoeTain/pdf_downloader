[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
markers =
    setup_mode: first-run tests with an empty user table
filterwarnings =
    ignore::DeprecationWarning
